Small inline loading spinner — used inside the scrape-in-progress banner.

```jsx
<Spinner />
```

No props. Orange top border on a translucent ring; spins continuously — reserve for short, bounded waits (scrape polling), never as a full-page blocker.
